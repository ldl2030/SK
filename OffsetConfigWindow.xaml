<Window x:Class="TestPlatform.OffsetConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="打印偏移配置" Height="520" Width="650"
        WindowStartupLocation="CenterOwner"
        FontFamily="Segoe UI" FontSize="12"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent">
    <Window.Resources>
        <DropShadowEffect x:Key="WindowShadow" BlurRadius="20" ShadowDepth="4" Opacity="0.3" Color="Black"/>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004C8C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#C0C0C0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#A0A0A0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeight" Value="35"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E5F0FF"/>
                    <Setter Property="Foreground" Value="#000"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>
    </Window.Resources>

    <Border Background="White" CornerRadius="16" Margin="10" Effect="{StaticResource WindowShadow}">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="⚙️" FontSize="28" Margin="0,0,10,0"/>
                    <TextBlock Text="打印偏移配置" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <Button x:Name="btnClose" Content="✕" Width="30" Height="30" HorizontalAlignment="Right"
                        Background="Transparent" Foreground="#888" BorderThickness="0" Cursor="Hand"
                        Click="BtnClose_Click" FontSize="16"/>
            </Grid>

            <Border Grid.Row="1" Background="#F0F7FF" CornerRadius="8" Padding="10" Margin="0,0,0,15">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="ℹ️" FontSize="14" Margin="0,0,8,0"/>
                    <TextBlock Text="X 偏移：正数向右，负数向左；Y 偏移：正数向下，负数向上。修改后保存立即生效，无需重启。" 
                               Foreground="#0078D7" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <DataGrid x:Name="dgProjects" Grid.Row="2" AutoGenerateColumns="False" 
                      CanUserAddRows="True" CanUserDeleteRows="True"
                      IsReadOnly="False" HeadersVisibility="Column"
                      Margin="0,0,0,20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="项目名称" Binding="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}" 
                                        Width="3*" IsReadOnly="False"/>
                    <DataGridTextColumn Header="X 偏移" Binding="{Binding XOffset, UpdateSourceTrigger=PropertyChanged}" 
                                        Width="1*" IsReadOnly="False"/>
                    <DataGridTextColumn Header="Y 偏移" Binding="{Binding YOffset, UpdateSourceTrigger=PropertyChanged}" 
                                        Width="1*" IsReadOnly="False"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnSave" Content="保存" Style="{StaticResource ModernButton}" Width="90" Height="34" 
                        Margin="0,0,12,0" Click="BtnSave_Click"/>
                <Button x:Name="btnCancel" Content="取消" Style="{StaticResource CancelButton}" Width="90" Height="34" 
                        Click="BtnCancel_Click"/>
            </StackPanel>

            <Border x:Name="loadingOverlay" Grid.RowSpan="4" Background="#AAFFFFFF" Visibility="Collapsed" CornerRadius="16">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="⏳" FontSize="40" Foreground="#0078D7"/>
                    <TextBlock Text="保存中..." Margin="0,10,0,0" FontSize="14" Foreground="#0078D7"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
        </EventTrigger>
    </Window.Triggers>
</Window>
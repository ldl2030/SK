<Window x:Class="TestPlatform.CableRotationMonitorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="旋转电缆测试" Height="300" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="请旋转产品或输出电缆360°" FontSize="18" FontWeight="SemiBold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="1" Text="正在监测频率，请缓慢旋转..." FontSize="14" 
                   HorizontalAlignment="Center" Foreground="#555" Margin="0,0,0,20"/>
        <Grid Grid.Row="2" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="当前频率：" FontSize="14" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock x:Name="tbFrequency" Grid.Column="1" FontSize="20" FontWeight="Bold" 
                       Foreground="#1976D2" Text="-- Hz" HorizontalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="3" Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="有效计数：" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock x:Name="tbCount" Grid.Column="1" Text="0/15" FontSize="16" FontWeight="Bold" 
                       Foreground="#4CAF50" HorizontalAlignment="Center"/>
            <Button Grid.Column="2" Content="取消" Width="80" Click="BtnCancel_Click" 
                    Background="#FF5B5B" Foreground="White" BorderThickness="0" Padding="10,4"/>
        </Grid>
        <ProgressBar Grid.Row="4" x:Name="progressBar" Height="8" Minimum="0" Maximum="16" 
                     Foreground="#4CAF50" Background="#E0E0E0" Margin="0,15,0,0"/>
    </Grid>
</Window>
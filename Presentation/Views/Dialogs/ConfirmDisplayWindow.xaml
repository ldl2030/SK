<Window x:Class="TestPlatform.ConfirmDisplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="确认" Width="560" MinHeight="340" MinWidth="440" MaxHeight="720"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True">
    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
        </Storyboard>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#0B5CAD"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="MinHeight" Value="42"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#084A8C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004C8C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="16" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.25" Color="Black"/>
        </Border.Effect>
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Button x:Name="btnClose"
                    Content="×"
                    Width="34"
                    Height="34"
                    Padding="0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    FontSize="22"
                    FontWeight="Bold"
                    Foreground="#5F6B7A"
                    Background="Transparent"
                    BorderThickness="0"
                    Cursor="Hand"
                    Panel.ZIndex="2"
                    Click="BtnClose_Click"/>

            <!-- 动态图标 -->
            <TextBlock Text="❓" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,15"/>

            <!-- 动态提示文本：限制正文高度，避免高DPI时挤掉底部操作按钮 -->
            <ScrollViewer Grid.Row="1"
                          MaxHeight="430"
                          Margin="0,0,0,20"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          CanContentScroll="False">
                <TextBlock x:Name="tbMessage" Text="请确认显示屏状态"
                           Foreground="#172033"
                           FontSize="18" FontWeight="Bold" TextAlignment="Center"
                           TextWrapping="Wrap"
                           Padding="4,0"/>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnYes" Content="确认" Style="{StaticResource ModernButton}" MinWidth="120" Margin="0,0,15,0" Click="BtnYes_Click"/>
                <Button x:Name="btnNo" Content="取消" Style="{StaticResource ModernButton}" MinWidth="120"
                        Background="#B42318" Foreground="White" Click="BtnNo_Click"/>
            </StackPanel>
        </Grid>
    </Border>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
        </EventTrigger>
    </Window.Triggers>
</Window>

<Window x:Class="TestPlatform.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="软件更新" Height="180" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" WindowStyle="SingleBorderWindow">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="tbTitle" Text="正在下载更新..." FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15" HorizontalAlignment="Center"/>
        <ProgressBar x:Name="progressBar" Grid.Row="1" Height="25" Minimum="0" Maximum="100" Value="0" Margin="0,0,0,10"/>
        <TextBlock x:Name="tbDetail" Grid.Row="2" Text="0%" FontSize="12" Foreground="Gray" HorizontalAlignment="Center"/>
    </Grid>
</Window>
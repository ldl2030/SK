<Window x:Class="TestPlatform.ScanCompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="条码比对" Height="320" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True">
    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>
        <Storyboard x:Key="Shake">
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" From="-10" To="10" Duration="0:0:0.05" AutoReverse="True" RepeatBehavior="2x"/>
        </Storyboard>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004C8C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="16" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.2"/>
        </Border.Effect>
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 动态图标 -->
            <Grid Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,15" Width="50" Height="50">
                <Ellipse Width="50" Height="50" Fill="#E8F0FE" Stroke="#0078D7" StrokeThickness="2" Opacity="0.8">
                    <Ellipse.RenderTransform>
                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                    </Ellipse.RenderTransform>
                </Ellipse>
                <TextBlock x:Name="IconText" Text="📷" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Grid>

            <!-- 动态提示文字 -->
            <TextBlock x:Name="tbStep" Grid.Row="1" Text="步骤1：请扫描主板镭雕码" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,15"/>

            <!-- 扫描输入框（居中） -->
            <TextBox x:Name="txtBarcode" Grid.Row="2" Height="40" FontSize="16" TextAlignment="Center" 
                     KeyDown="TxtBarcode_KeyDown" BorderBrush="#CCCCCC" BorderThickness="1" 
                     Background="#F9F9F9" CaretBrush="#0078D7" Margin="0,0,0,10"/>

            <!-- 动态状态文字 -->
            <TextBlock x:Name="tbStatus" Grid.Row="3" Text="等待扫描..." Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,15" FontSize="12"/>

            <!-- 取消按钮 -->
            <Button x:Name="btnCancel" Grid.Row="4" Content="取消" Width="100" Height="35" HorizontalAlignment="Center"
                    Style="{StaticResource ModernButton}" Background="#E0E0E0" Foreground="Black" Click="BtnCancel_Click"/>
        </Grid>
    </Border>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
        </EventTrigger>
    </Window.Triggers>
</Window>
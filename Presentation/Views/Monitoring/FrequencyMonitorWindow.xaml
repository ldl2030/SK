<Window x:Class="TestPlatform.FrequencyMonitorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="频率检测" Height="380" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#F5F5F5">
    <Window.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
        </Storyboard>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D7"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="20">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004C8C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="频率检测" FontSize="20" FontWeight="SemiBold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="#333"/>

        <Border Grid.Row="1" Background="#E8F0FE" CornerRadius="8" Padding="10" Margin="0,0,0,15">
            <TextBlock x:Name="tbStatus" Text="正在检测频率，请稍候..." 
                       FontSize="14" Foreground="#0078D7" TextWrapping="Wrap"
                       HorizontalAlignment="Center"/>
        </Border>

        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="当前频率：" FontSize="16" VerticalAlignment="Center" Foreground="#555"/>
            <TextBlock x:Name="tbFrequency" Grid.Column="1" Text="-- Hz" FontSize="22" 
                       FontWeight="Bold" Foreground="#0078D7" TextAlignment="Right"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="有效次数：" FontSize="14" VerticalAlignment="Center" Foreground="#555"/>
            <TextBlock x:Name="tbValidCount" Grid.Column="1" Text="0" FontSize="16" 
                       FontWeight="Bold" Foreground="#0078D7" TextAlignment="Right"/>
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="tbCountdown" Text="剩余时间: 30 秒" 
                       FontSize="14" Foreground="#666" HorizontalAlignment="Center"/>
            <ProgressBar x:Name="progressBar" Grid.Row="1" Height="8" Minimum="0" Maximum="30" 
                         Value="30" Foreground="#4CAF50" Background="#E0E0E0" Margin="0,10,0,0"/>
        </Grid>

        <Button x:Name="btnCancel" Grid.Row="5" Content="取消" Width="100" Height="35" 
                HorizontalAlignment="Center" Click="BtnCancel_Click" Style="{StaticResource ModernButton}"/>
    </Grid>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource FadeIn}"/>
        </EventTrigger>
    </Window.Triggers>
</Window>